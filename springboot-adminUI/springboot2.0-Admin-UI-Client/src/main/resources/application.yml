###配置client注册到serve（admin ui平台）上
spring:
  boot:
    admin:
      client:
        url: http://localhost:8080
        
###指定启动端口号
server:
  port: 8081 ###端口号
  
###通过下面的配置启用所有的监控端点，默认情况下，这些端点是禁用的；
management:
  endpoints:
    web:
      exposure:
        include: "*"
